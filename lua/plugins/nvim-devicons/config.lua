local utils = require('utils')
local invariant_require = utils.invariant_require
local nvim_web_devicons = invariant_require('nvim-web-devicons')

if nvim_web_devicons then nvim_web_devicons.setup({}) end
